---
name: audit-2026-06-21
description: Findings từ claude-md-improver audit ngày 21/06/2026 — 4 file doc có drift version (React 18/Tailwind sai) + MEMORY.md có entry ma
metadata:
  type: reference
---

## Audit bằng skill `claude-md-management:claude-md-improver` ngày 21/06/2026

### Files đã audit (4 files)

| File | Score trước | Score sau (ước tính) | Loại fix |
|---|---|---|---|
| `CLAUDE.md` | 71/100 (B) | ~83/100 (B+) | Version drift: React 18→19, Tailwind v4→CSS Modules, npm test watch→single-run, thiếu 4 skills |
| `INSTRUCTIONS.md` | 82/100 (B) | ~88/100 (B+) | Version drift giống CLAUDE.md + storage rule sync |
| `files/EXTENSION_SPEC.md` | 58/100 (C) | ~68/100 (C+) | React 18→19, Framer Motion 11→12, date-fns v3→4, section 6 directory tree sai (caveat added) |
| `memory/*.md` (8 files) | ~80/100 | ~85/100 | MEMORY.md có entry ma (SonarQube), project_status.md có Tailwind, reference_skills.md thiếu 2 custom skills |

### Pattern lỗi chính: **Stack version drift**

Cả 3 file CLAUDE.md / INSTRUCTIONS.md / EXTENSION_SPEC.md đều ghi "React 18 + Tailwind CSS v4" nhưng `package.json` thực tế là React **19**, không có Tailwind. Nguyên nhân: docs được viết lúc scaffold project (WXT starter template), sau đó owner upgrade React nhưng quên sync docs.

### Lỗi cấu trúc: **MEMORY.md entry ma**

`MEMORY.md` dòng 10 trỏ tới `feedback_sonarqube_post_task.md` — file này **không tồn tại** trong repo. Có thể file đã bị xóa nhưng index không được cleanup. Đã xóa entry, thay bằng entry mới trỏ tới file audit này.

### Lỗi section 6 EXTENSION_SPEC: **Directory tree không khớp code thật**

Section 6 liệt kê `src/options/`, `src/offscreen/`, `tailwind.config.ts`, `.eslintrc.json`, `.prettierrc`, `tests/e2e/` — tất cả đều **chưa tồn tại** trong codebase hiện tại. Đã thêm caveat warning ở đầu section thay vì restructure toàn bộ (vì section này đúng cho target state).

### Lỗi skill: **reference_skills.md thiếu custom skills**

Đã thêm `harness-bootstrap` và `harness-mistake-log` (2 custom skills theo MEMORY.md dòng 12 đã reference nhưng file không liệt kê).

### Edits applied (tổng 13)

1. CLAUDE.md: version bump + memory read rule + 4 skill thiếu + stack fix + npm test fix + Firefox scripts
2. INSTRUCTIONS.md: version bump + React 19 + storage rule fix
3. EXTENSION_SPEC.md: version bump + stack versions fix + section 6 caveat
4. MEMORY.md: xóa entry SonarQube ma + thêm entry audit này
5. project_status.md: Tailwind→CSS Modules
6. reference_skills.md: thêm 2 custom skills

### Lessons learned (feedback cho harness)

**Why:** Drift docs thường xảy ra khi code thay đổi (dependency upgrade, refactor) mà owner quên sync. Đây là **mistake cần log + engineer fix ở layer nào?**

**Có thể engineer ở L7 (Quality Gates):** thêm hook PostToolUse check mismatch giữa `package.json` dependencies và claim trong `CLAUDE.md` / `INSTRUCTIONS.md` / `EXTENSION_SPEC.md`. Nhưng scope rộng, cần owner approve.

**Trước mắt (low-cost fix):** Sau mỗi lần `npm install` upgrade dependency → check 3 file doc trên và sync.

**How to apply:** Mỗi session, đọc file này + MEMORY.md index để biết đã audit gì, đã fix gì, còn issue nào.

Liên quan: [[reference-harness-framework]] (7 layers), [[project-status]] (current state)