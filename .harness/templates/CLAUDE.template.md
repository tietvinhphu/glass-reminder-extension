# {{PROJECT_NAME}} — Claude Code Instructions
> Version 1.0 | {{DATE}}

## ⚡ ĐỌC NGAY KHI BẮT ĐẦU SESSION

Đọc các file này trước khi nhận bất kỳ task nào:
- `memory/MEMORY.md` + tất cả files trong `memory/` — **bộ nhớ xuyên session**, đọc đầu tiên
- `INSTRUCTIONS.md` — coding standards, security rules, workflow
- `files/SPEC.md` — full technical spec
- `HARNESS.md` (nếu có) — framework meta-rules

> **Môi trường đặc biệt:** [Mô tả môi trường đặc biệt, vd: "Laptop domain cty xóa dữ liệu local khi tắt máy"]
> Memory được lưu trong repo GitHub tại `memory/`. Đây là source of truth duy nhất.

---

## 🧠 SKILL SELECTION — Tự động chọn skill trước khi làm task

Trước khi bắt đầu BẤT KỲ task nào, kiểm tra skills có sẵn trong `.agents/skills/` và chọn skill phù hợp.

**Quy tắc:** Đọc SKILL.md của skill được chọn trước khi bắt đầu implement.

---

## 🎯 Project

- **Tên:** {{PROJECT_NAME}}
- **Mục đích:** {{PURPOSE}}
- **Stack:** {{STACK}}
- **Deploy:** {{DEPLOY_TARGET}}

---

## 🗺️ Vai trò trong team

[Customize theo workflow của anh — vd: solo, có team, có Cursor Cloud agent không]

```
CLAUDE       → Điều phối, lên plan, dạy concept
[ROLE_2]     → [Mô tả]
OWNER        → Review, quyết định push
LOCAL IDE    → Quality gate
GITHUB MAIN  → Source of truth
```

---

## 💬 Quy tắc bắt buộc khi viết code

### 1. Comment giải thích — LUÔN CÓ
Mọi function, block logic phức tạp PHẢI có comment giải thích mục đích, lý do, param/return.

### 2. TypeScript strict — no any
### 3. [Security rule 1]
### 4. [Security rule 2]
### 5. [Security rule 3]

---

## 🔴🟢🔵 TDD — RED GREEN REFACTOR

```
🔴 RED    → Viết test → chạy → FAIL
🟢 GREEN  → Implement → chạy → PASS
🔵 REFACTOR → Clean + comment → tests vẫn PASS
```

---

## 🔍 POST-TASK BẮT BUỘC — Query SonarQube và fix sạch

Sau khi implement xong bất kỳ task nào có chỉnh sửa code:
1. Query SonarQube (MCP hoặc CLI)
2. Fix TẤT CẢ issues
3. Re-analyze đến khi 0 issues
4. CHỈ SAU ĐÓ mới báo task hoàn thành

---

## 📋 Output format khi nhận task

```
## Task: [Tên feature]

### 🔴 Tests cần viết TRƯỚC
- [ ] test case 1
- [ ] test case 2

### 🟢 Implementation plan
- [ ] file cần tạo
- [ ] logic cần implement

### ✅ Done khi
- [ ] npm test → xanh
- [ ] npm run type-check → pass
- [ ] Manual test → tính năng hoạt động
```

---

## 🚀 Commands

```bash
[Liệt kê các lệnh thường dùng, vd: dev, test, build, deploy]
```

---

*Template version 1.0 | Generated by Harness Engineering framework*
